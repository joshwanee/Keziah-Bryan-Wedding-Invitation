import { useState } from 'react';import { AnimatePresence,motion } from 'framer-motion';import { FiChevronDown } from 'react-icons/fi';import { weddingData } from '../../data/weddingData';
export default function FAQ(){const [open,setOpen]=useState(0);return <section id="faq" className="bg-ivory"><div className="section-shell"><p className="eyebrow">Good to know</p><h2 className="section-title">Frequently Asked Questions</h2><div className="mx-auto mt-10 max-w-3xl space-y-3">{weddingData.faqs.map((item,i)=><div key={item.q} className="glass rounded-2xl"><button onClick={()=>setOpen(open===i?-1:i)} className="flex w-full items-center justify-between gap-4 p-5 text-left font-medium text-teal"><span>{item.q}</span><FiChevronDown className={`shrink-0 transition ${open===i?'rotate-180':''}`}/></button><AnimatePresence>{open===i&&<motion.p initial={{height:0,opacity:0}} animate={{height:'auto',opacity:1}} exit={{height:0,opacity:0}} className="overflow-hidden px-5 pb-5 text-sm leading-7 text-teal/65">{item.a}</motion.p>}</AnimatePresence></div>)}</div></div></section>}
